// stb_ds is header-only: exactly one translation unit must define the
// implementation macro. Keeping it alone in this file means the benchmark
// sources can include stb_ds.h freely.
#define STB_DS_IMPLEMENTATION
#include "stb_ds.h"
